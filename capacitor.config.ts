import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vaccarov.apptest',
  appName: 'app-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
